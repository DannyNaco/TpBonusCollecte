# Frontend React - Game Collection (Retro Style)

Interface utilisateur moderne avec une touche vintage pour gérer votre collection de jeux.

## 🎨 Design System (Retro 70s / Outer Banks)
- **Couleurs** : Sable, Orange Sunset, Teal Profond.
- **Polices** : `Shrikhand` (Titres) et `DM Sans` (Texte).
- **Framework** : TailwindCSS v3.

## 🚀 Commandes
- `npm run dev` : Lancer le serveur de développement.
- `npm run build` : Compiler pour la production.
- `npm run preview` : Prévisualiser la production.

## ⚙️ Configuration
L'API est configurée par défaut sur `http://localhost:3000/api` dans `src/services/api.js`.
